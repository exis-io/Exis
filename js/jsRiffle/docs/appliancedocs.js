/**
 * @memberof Auth
 * @function delete_custom_token
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.delete_custom_token(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function gen_custom_token
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.gen_custom_token(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function get_custom_token
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.get_custom_token(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function get_private_data
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.get_private_data(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function get_public_data
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.get_public_data(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function get_users
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.get_users(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function get_user_data
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.get_user_data(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function list_custom_tokens
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.list_custom_tokens(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function save_user_data
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.save_user_data(...args).then(success, error);
 */

/**
 * @memberof Auth
 * @function user_count
 * @see {@link /docs/appliances/Auth here} for documentation.
 * @example
 * auth.user_count(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function addDevModeDomain
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.addDevModeDomain(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function addDynamicRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.addDynamicRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function addSpecialAgent
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.addSpecialAgent(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function addStaticRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.addStaticRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function assignDynamicRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.assignDynamicRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function assignRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.assignRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function checkPerm
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.checkPerm(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function delDynamicRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.delDynamicRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function destroyRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.destroyRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function inDevModeStatus
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.inDevModeStatus(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function listMembers
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.listMembers(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function listRoles
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.listRoles(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function listSpecialAgents
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.listSpecialAgents(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function newDynamicRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.newDynamicRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function removeApp
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.removeApp(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function removeDevModeDomain
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.removeDevModeDomain(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function removeSpecialAgent
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.removeSpecialAgent(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function revokeDynamicRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.revokeDynamicRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function revokePerm
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.revokePerm(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function revokeRole
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.revokeRole(...args).then(success, error);
 */

/**
 * @memberof Bouncer
 * @function setPerm
 * @see {@link /docs/appliances/Bouncer here} for documentation.
 * @example
 * bouncer.setPerm(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function build
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.build(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function create
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.create(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function list
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.list(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function images
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.images(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function remove
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.remove(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function removeImage
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.removeImage(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function updateImage
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.updateImage(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function image
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.image(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function inspect
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.inspect(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function logs
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.logs(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function restart
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.restart(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function start
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.start(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function stop
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.stop(...args).then(success, error);
 */

/**
 * @memberof Container
 * @function top
 * @see {@link /docs/appliances/Container here} for documentation.
 * @example
 * container.top(...args).then(success, error);
 */

/**
 * @memberof Replay
 * @function addReplay
 * @see {@link /docs/appliances/Replay here} for documentation.
 * @example
 * replay.addReplay(...args).then(success, error);
 */

/**
 * @memberof Replay
 * @function removeReplay
 * @see {@link /docs/appliances/Replay here} for documentation.
 * @example
 * replay.removeReplay(...args).then(success, error);
 */

/**
 * @memberof Replay
 * @function pauseReplay
 * @see {@link /docs/appliances/Replay here} for documentation.
 * @example
 * replay.pauseReplay(...args).then(success, error);
 */

/**
 * @memberof Replay
 * @function getReplay
 * @see {@link /docs/appliances/Replay here} for documentation.
 * @example
 * replay.getReplay(...args).then(success, error);
 */

