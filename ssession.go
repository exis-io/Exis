package riffle

// An unfortunately named object that wraps the session.
// I'll change the name once I clean up the existing code
type Ssession struct {
	Session
}

// func NesSession(domain string) (*Ssession, error) {
//     client, err =
// 	return &Session{}
// }
